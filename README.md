# Dina Radosavlevich — Portfolio

A responsive static portfolio website based on the Figma design.

## Preview locally

Open `index.html` directly, or run a local server:

```sh
python3 -m http.server 8080
```

Then visit http://localhost:8080.
